1.算子链（可以分开，也可以串联，类似于sparkRDD的阶段切分）disableChaining、disableOperatorChaining（全局）
2、跟connector相关的算子都得导入依赖
3、max与maxby，max保留第一次by的其他属性数据，而maxby会取新的max的数据
4、窗口：（时间=》滑动窗口，会话窗口，滚动窗口）、（事件=》滑动、滚动）
5、窗口的生命周期（第一个窗口驱动来时创建，window单例集合，）
6、迟到数据处理=》(1、推迟水位线进度，2、允许窗口延时关闭,3、极端情况-侧输出流)
7、乱序（小的在大的后面）、迟到（延迟到的数据）